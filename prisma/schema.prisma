model Report {
  id         Int      @id @default(autoincrement())
  message    String
  sentiment  String
  confidence Float
  reasoning  String?
  emotions   Json?
  source     String
  userId     String?
  channelId  String?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}